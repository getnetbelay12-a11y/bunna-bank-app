export enum ActivityType {
  CUSTOMER_HELPED = 'customer_helped',
  DEPOSIT = 'deposit',
  WITHDRAWAL = 'withdrawal',
  TRANSFER = 'transfer',
  SCHOOL_PAYMENT = 'school_payment',
  LOAN_CREATED = 'loan_created',
  LOAN_REVIEWED = 'loan_reviewed',
  LOAN_APPROVED = 'loan_approved',
  LOAN_REJECTED = 'loan_rejected',
  LOAN_FORWARDED = 'loan_forwarded',
  LOAN_FOLLOWUP = 'loan_followup',
  VOTE_MANAGED = 'vote_managed',
}
